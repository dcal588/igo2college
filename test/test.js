//funcs
const expect = require("chai").expect;

const should = require("chai").should;

const normalize = require("../functions");

//describe

describe("normalize", () => {
  it ("should take strings for numbers and parseInt");
});
